<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.disword.diswordlib">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SwipeListActivity" />
        <activity android:name=".RxbindingActivity" />
        <activity android:name=".demo.pages.IndexActivity" />
        <activity
            android:name=".demo.pages.auth.LoginActivity"
            android:windowSoftInputMode="adjustResize|stateHidden" />
        <activity android:name=".demo.pages.viewpager.LoopPagerActivity" />
        <activity android:name=".demo.pages.search.SearchListActivity"></activity>
    </application>

</manifest>